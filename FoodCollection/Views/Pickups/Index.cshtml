@* @model IEnumerable<FoodCollection.Models.Pickup>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PickupStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BookPickup)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Staff)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PickupStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookPickup.BookPickupId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Staff.StaffId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PickupId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PickupId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PickupId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
 *@

@model IEnumerable<FoodCollection.Models.Pickup>

@{
    ViewData["Title"] = "Pickups";
}

<link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.11.3/font/bootstrap-icons.min.css" />

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h3 class="mb-0"><i class="bi bi-truck me-2 text-success"></i>Pickups</h3>
    </div>

    <div class="card shadow-lg border-0 rounded-4">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-success">
                        <tr>
                            <th>@Html.DisplayNameFor(model => model.Date)</th>
                            <th>@Html.DisplayNameFor(model => model.PickupStatus)</th>
                            <th>@Html.DisplayNameFor(model => model.BookPickup)</th>
                            <th>@Html.DisplayNameFor(model => model.Staff)</th>
                            <th class="text-center" style="width: 200px;">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (!Model.Any())
                        {
                            <tr>
                                <td colspan="5" class="text-center text-muted py-4">
                                    <i class="bi bi-exclamation-circle me-1"></i>No pickups found.
                                </td>
                            </tr>
                        }
                        else
                        {
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(modelItem => item.Date)</td>
                                    <td>@Html.DisplayFor(modelItem => item.PickupStatus)</td>
                                    <td>@Html.DisplayFor(modelItem => item.BookPickup.BookPickupId)</td>
                                    <td>@Html.DisplayFor(modelItem => item.Staff.StaffId)</td>
                                    <td class="text-center">
                                        <a asp-action="Details" asp-route-id="@item.PickupId" class="btn btn-sm btn-outline-info me-1" title="Details">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                        <a asp-action="Edit" asp-route-id="@item.PickupId" class="btn btn-sm btn-outline-primary me-1" title="Edit">
                                            <i class="bi bi-pencil-square"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.PickupId" class="btn btn-sm btn-outline-danger" title="Delete">
                                            <i class="bi bi-trash"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>